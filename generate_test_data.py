import pandas as pd

def generate_test_files():
    print("Генерация тестовых файлов...")

    # --- 1. Файл: Часы_Класс_Предмет.xlsx ---
    # Пример данных
    classes = ['5А', '5Б', '6А', '6Б', '7А']
    subjects = ['Математика', 'Русский язык', 'Казахский язык', 'Физическая культура', 'Информатика', 'История РК']

    # Создаём словарь для DataFrame
    data_hours = {}
    data_hours['Класс/Предмет'] = classes

    for subject in subjects:
        # Пример: случайное количество часов, можно сделать по-разному
        # Для простоты, фиксированные значения
        if subject == 'Математика':
            hours = [5, 5, 5, 5, 4]
        elif subject == 'Русский язык':
            hours = [4, 4, 4, 4, 4]
        elif subject == 'Казахский язык':
            hours = [2, 2, 2, 2, 2]
        elif subject == 'Физическая культура':
            hours = [3, 3, 3, 3, 3]
        elif subject == 'Информатика':
            hours = [2, 2, 0, 0, 0] # Только 5 классы
        elif subject == 'История РК':
            hours = [2, 2, 2, 2, 2]
        else:
            # Для остальных предметов ставим 0 или случайные значения
            hours = [0] * len(classes)

        data_hours[subject] = hours

    df_hours = pd.DataFrame(data_hours)
    df_hours.set_index('Класс/Предмет', inplace=True) # Устанавливаем классы как индекс

    # Сохраняем в Excel
    df_hours.to_excel('Часы_Класс_Предмет.xlsx', index=True, engine='openpyxl')
    print("Файл 'Часы_Класс_Предмет.xlsx' создан.")


    # --- 2. Файл: Учителя_Кабинеты_Предметы_Классы.xlsx ---
    # Пример данных с делением на подгруппы (через разные строки)
    data_teachers = {
        'Учитель': [
            'Давлетиярова А.К.', 'Баер Э.В.', 'Юбилеева Д.С.', 'Габбасова С.Х.',
            'Смолина Т.Н.', 'Шахимова Ы.А.', 'Петрова П.П.', 'Иванов И.И.', # Добавлены для примера
            'Козлова К.К.', 'Морозов М.М.' # Добавлены для примера
        ],
        'Предмет': [
            'Математика', 'Математика', 'Русский язык', 'Музыка',
            'Информатика', 'Информатика', 'Физическая культура', 'Русский язык', # Пример нагрузки для Петровой
            'Химия', 'Физика' # Пример нагрузки для Козловой и Морозова
        ],
        'Класс': [
            '5А', '5Б', '5А', '5А',
            '5А', '5А', '5А', '6А', # Петрова П.П. ведёт у 6А
            '7А', '7А' # Козлова и Морозов в 7А
        ],
        'Кабинет': [
            '45', '43', '33', '41',
            '22', '26', 'с\з', '34', # Кабинет для Петровой в 6А
            '42', 'физика' # Кабинеты для Козловой и Морозова
        ],
        'Количество_часов_в_неделю': [
            3, 2, 4, 1, # Сумма по математике в 5А = 5, в 5Б = 2, русский в 5А = 4, музыка = 1
            1, 1, 3, 2, # Информатика 5А-1 (Смолина) = 1, 5А-2 (Шахимова) = 1, физкультура 5А = 3, русский 6А = 2
            2, 2 # Химия и физика по 2 часа
        ]
    }

    df_teachers = pd.DataFrame(data_teachers)

    # Сохраняем в Excel
    df_teachers.to_excel('Учителя_Кабинеты_Предметы_Классы.xlsx', index=False, engine='openpyxl')
    print("Файл 'Учителя_Кабинеты_Предметы_Классы.xlsx' создан.")

    print("Тестовые файлы успешно сгенерированы.")


if __name__ == "__main__":
    generate_test_files()
