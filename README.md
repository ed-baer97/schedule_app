# Система составления расписания для школ

Веб-приложение для управления расписанием занятий в школах с поддержкой нескольких школ и автоматической генерацией расписания.

## Основные возможности

### 1. Управление расписанием
- Составление постоянного расписания для смен
- Временные изменения расписания
- Автоматическая генерация расписания с помощью специализированного алгоритма
- Поддержка подгрупп (несколько учителей для одного предмета в классе)

### 2. Управление данными
- Классы и смены
- Предметы и нагрузка классов
- Учителя и их назначения
- Кабинеты и закрепление за учителями

### 3. Многопользовательская система
- Поддержка нескольких школ
- Роли: супер-администратор и администратор школы
- Изоляция данных между школами

### 4. Telegram интеграция
- Отправка расписания учителям
- Уведомления о временных изменениях
- Поддержка отдельных ботов для каждой школы

## Архитектура

### Алгоритм составления расписания

Система использует специализированный алгоритм на основе constraint satisfaction (решения задач с ограничениями) вместо LLM.

**Преимущества:**
- ⚡ Скорость: работает в миллисекундах
- ✅ Надежность: гарантированно соблюдает все ограничения
- 🎯 Детерминированность: предсказуемый результат
- 💰 Не требует API: работает локально
- 📦 Меньше ресурсов: не требует GPU

**Ограничения, которые учитывает алгоритм:**
1. Учитель не может быть в двух местах одновременно
2. Кабинет не может быть занят двумя классами одновременно
3. Количество уроков в день не превышает максимум
4. Подгруппы размещаются одновременно (один день, один урок)

**Основные компоненты:**
- `app/services/schedule_solver.py` - основной алгоритм
- `app/services/schedule_solver_adapter.py` - адаптер для интеграции с БД

Подробнее: [docs/SCHEDULE_SOLVER.md](docs/SCHEDULE_SOLVER.md)

### База данных

Система использует две базы данных:
1. **system.db** - системная БД (школы, пользователи)
2. **databases/school_<id>.db** - отдельная БД для каждой школы

**Модели данных:**
- `app/models/system.py` - системные модели (School, User)
- `app/models/school.py` - модели школы (ClassGroup, Subject, Teacher, Schedule, etc.)

### Структура проекта

```
schedule_app/
├── app/
│   ├── core/           # Ядро приложения (auth, db_manager, config)
│   ├── models/         # Модели БД
│   ├── routes/         # Маршруты (blueprints)
│   └── services/       # Бизнес-логика (solver, excel_loader, telegram_bot)
├── templates/          # HTML шаблоны
├── utils/              # Утилиты (prompt_db, id_normalizer)
├── databases/          # Базы данных школ
├── docs/               # Документация
└── tests/              # Тесты
```

## Установка и запуск

### Требования
- Python 3.8+
- Flask
- SQLAlchemy
- pandas (для загрузки Excel)

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Инициализация
```bash
python init_system.py  # Создание системной БД и первого супер-админа
```

### Запуск
```bash
python app.py
```

Приложение будет доступно по адресу `http://localhost:5000`

## Использование

### Супер-администратор

Супер-администратор может:
- Создавать и удалять школы
- Создавать администраторов для школ
- Очищать данные школ
- Настраивать Telegram ботов для школ

**Роуты:**
- `/super-admin` - панель управления
- `POST /super-admin/schools/create` - создание школы
- `POST /super-admin/schools/<id>/delete` - удаление школы
- `POST /super-admin/schools/<id>/clear` - очистка данных школы
- `POST /super-admin/schools/<id>/telegram-token` - настройка Telegram бота

Подробнее: [SUPER_ADMIN_FEATURES.md](SUPER_ADMIN_FEATURES.md)

### Администратор школы

Администратор может:
- Управлять классами, предметами, учителями
- Загружать данные из Excel
- Составлять расписание
- Генерировать расписание автоматически
- Отправлять расписание учителям через Telegram

**Основные страницы:**
- `/admin` - главная страница
- `/admin/schedule` - управление расписанием
- `/admin/classes` - управление классами
- `/admin/subjects` - управление предметами
- `/admin/teachers` - управление учителями
- `/admin/cabinets` - управление кабинетами
- `/admin/upload` - загрузка данных из Excel

## Генерация расписания

### Автоматическая генерация

1. Перейдите на страницу `/admin/schedule`
2. Выберите смену
3. Нажмите кнопку "Сгенерировать расписание"
4. Система автоматически создаст расписание с учетом всех ограничений
5. Просмотрите предложения и примените их

### Ручное составление

1. Перейдите на страницу `/admin/schedule`
2. Выберите смену
3. Кликните на ячейку расписания
4. Выберите предмет, учителя и кабинет
5. Сохраните изменения

## Загрузка данных из Excel

Система поддерживает загрузку данных из Excel файлов:

1. **Часы_Класс_Предмет.xlsx** - нагрузка классов по предметам
   - Формат: строки = классы, столбцы = предметы, ячейки = часы в неделю
   - Может содержать несколько листов (по одному на смену)

2. **Учителя_Предмет.xlsx** - назначение учителей на предметы и классы
   - Колонки: Учитель, Предмет, Класс, Кабинет, Количество_часов_в_неделю

3. **Учителя_Контакты.xlsx** - контакты учителей
   - Колонки: Учитель, Telegram ID

4. **Учителя_Кабинет.xlsx** - закрепление кабинетов за учителями
   - Колонки: Учитель, Кабинет

## Telegram интеграция

### Настройка бота для школы

1. Создайте бота через @BotFather в Telegram
2. Получите токен
3. В панели супер-администратора настройте токен для школы

### Отправка расписания

Система может отправлять:
- Постоянное расписание всем учителям
- Временные изменения расписания
- Индивидуальные расписания учителям

**Роуты:**
- `POST /admin/telegram/send-schedule` - отправить расписание всем
- `POST /admin/telegram/send-temporary` - отправить временные изменения

Подробнее: [docs/telegram_bot_per_school.md](docs/telegram_bot_per_school.md)

## База данных промптов

Система использует базу данных промптов для хранения структуры "Класс → Предмет → Учителя" и автоматического определения подгрупп.

**Правила определения подгрупп:**
- Если в классе и предмете 2+ учителя → есть подгруппы
- Если только 1 учитель → подгрупп нет

**Модели:**
- `PromptClassSubject` - класс, предмет, общие часы, наличие подгрупп
- `PromptClassSubjectTeacher` - связь с учителями, индивидуальная нагрузка

Подробнее: [docs/prompt_database.md](docs/prompt_database.md)

## API

### Основные эндпоинты

**Расписание:**
- `GET /admin/schedule/data` - получить расписание
- `POST /admin/schedule/save` - сохранить урок
- `POST /admin/schedule/clear` - очистить расписание

**Управление данными:**
- `GET /admin/classes` - список классов
- `GET /admin/subjects` - список предметов
- `GET /admin/teachers` - список учителей
- `POST /admin/upload` - загрузка Excel

Полный список API доступен в `api.py`

## Разработка

### Структура кода

- **Blueprints**: маршруты организованы в blueprints (`app/routes/`)
- **Services**: бизнес-логика вынесена в сервисы (`app/services/`)
- **Models**: модели БД в `app/models/`
- **Core**: ядро приложения (auth, db_manager) в `app/core/`

### Добавление новой функциональности

1. Создайте blueprint в `app/routes/` или добавьте роуты в существующий
2. Зарегистрируйте blueprint в `api.py`
3. Создайте сервис в `app/services/` для бизнес-логики
4. Добавьте модели в `app/models/` при необходимости

## Лицензия

Проект разработан для внутреннего использования.

## Поддержка

При возникновении проблем:
1. Проверьте логи приложения
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки БД в `app/core/config.py`

